# DESIGN.md — 디자인 시스템 & 코딩 규칙

## 스타일 방향
- **프로덕트 타입**: 대시보드 + 게이밍 하이브리드
- **스타일**: 다크 모드 + 노을 감성 (따뜻한 게더타운)
- **톤**: 딱딱한 사내 시스템이 아닌, 들어오고 싶은 공간

## 색상
```css
:root {
  /* 배경 */
  --bg-primary: #1a1118;
  --bg-secondary: #2a1f28;
  --bg-tertiary: #3d2f38;

  /* 액센트 */
  --accent: #E8725C;        /* 코랄 */
  --accent-light: #F4A58A;

  /* 텍스트 */
  --text-primary: #F0E6E0;
  --text-secondary: #D4C4BC;
  --text-muted: #A8948A;

  /* 시맨틱 */
  --success: #5DC878;
  --warning: #F0A830;
  --danger: #E84360;
  --info: #5CAED6;
}
```

## 타이포그래피
| 용도 | 폰트 | 근거 |
|------|------|------|
| 헤딩 | **DungGeunMo** | 레트로 게임 감성 |
| 본문 | **Galmuri11** | 한국어 지원, 픽셀 느낌 |
| 숫자/코드 | **Galmuri11** | KPI 숫자 정렬 |

```
text-xs:   12px  — 타임스탬프, 메타
text-sm:   14px  — 라벨, 보조
text-base: 16px  — 본문 (모바일 최소)
text-lg:   18px  — 카드 제목
text-xl:   20px  — 섹션 제목
text-2xl:  24px  — 페이지 제목
```

## 컴포넌트 규칙
| 규칙 | 적용 |
|------|------|
| 터치 타겟 | 모든 인터랙티브 요소 최소 44x44px |
| 호버 피드백 | 색상/그림자 변경 (scale 사용 금지) |
| 트랜지션 | `transition-colors duration-200` |
| 포커스 링 | `focus-visible:ring-2 ring-accent` |
| 스켈레톤 | 데이터 로딩 시 스켈레톤 스크린 |
| 에러 표시 | 입력 필드 아래 빨간 텍스트 |
| 빈 상태 | 이모지 + 안내 텍스트 (예: "아직 VOC가 없어요 📭") |

## 아이콘
- **Lucide React** 사용 (이모지 아이콘 금지 — Zone 라벨 등 장식 목적은 예외)
- 일관된 크기: `w-5 h-5` (기본), `w-4 h-4` (소형)

## 코딩 규칙 (SHOULD)
- 컴포넌트 분리는 응집도 기준 — 역할이 다를 때 분리 (줄 수 기계적 제한 없음)
- Supabase 쿼리(select/insert/update)는 `hooks/`에 집중, Storage·단발성 호출은 컴포넌트 내 허용
- Zustand 스토어 도메인별 분리
- 타입은 `types/database.ts`에 수동 정의 (현 프로젝트 기준)
- 모바일 터치 타겟 44x44px 이상

## 코딩 규칙 (PRACTICE)
- Tailwind 유틸리티 우선, 커스텀 CSS 최소
- 로딩 → 스켈레톤, 에러 → 인라인 메시지, 빈 상태 → 이모지+안내
- 낙관적 업데이트 (투표, 읽음 처리 등)
- 한국어 UI, 코드 주석 한국어 OK
- `transition-colors duration-200` 기본 적용

## 공통 규칙 (PC + 모바일)
- 터치 타겟 최소 44x44px
- 모든 인터랙티브 요소에 `cursor-pointer`
- 트랜지션 150~300ms
- 로딩 시 스켈레톤 스크린
- 에러 메시지는 문제 발생 위치 근처에 표시

## 상태 뱃지 색상
| 상태 | 색상 | 의미 |
|------|------|------|
| 접수 | 회색 | 아직 확인 안 함 |
| 검토중 | 파랑 | 리더가 확인 중 |
| 처리중 | 주황 | 담당자 배정, 진행 중 |
| 완료 | 초록 | 처리 완료 |
| 보류 | 빨강 | 사유와 함께 보류 |

## 시급성 3단계
| 시급성 | 의미 | 알림 방식 |
|--------|------|-----------|
| 🔴 **긴급** | 즉시 확인 필요 | 수집함 + 홈 배너 |
| 🟡 **할일** | 기한 내 확인 | 수집함 |
| 🔵 **참고** | 참고 사항 | 수집함 (낮은 우선순위) |
